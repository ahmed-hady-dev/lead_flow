import 'package:flutter/material.dart';

class PaymentSuccessSection extends StatelessWidget {
  const PaymentSuccessSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
